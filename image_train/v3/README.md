# image_train v3 代码

入口：`image_train/v3/train_image_v3.py`。本版本复用 v2 ROI cache，并在 val 上选择段级后处理参数。

完整命令、指标和分析见仓库根相对路径 `experiments/03_rgb_roi/03_v3_segment_postprocess/README.md`；
原始结果见 `image_train/image_train_v3.md` 和 `image_train/output_v3/image_train_v3.json`。
